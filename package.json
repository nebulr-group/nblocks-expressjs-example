{
  "name": "app",
  "version": "1.0.0",
  "description": "* Barebone template to use for any project. * Contains only NodeJS. * Integrated with VS Code so it can be opened/built straight from VS Code. * Contains `.bashrc` to set env variables",
  "main": "index.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
